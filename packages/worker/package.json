{
  "name": "@bingmail/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "postal-mime": "^2.4.3"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.19.0"
  },
  "scripts": {
    "dev": "bun ./scripts/wrangler-dev.ts",
    "deploy": "wrangler deploy",
    "typecheck": "bunx tsc -p tsconfig.json",
    "db:migrate": "bun ./scripts/db-migrate.ts",
    "db:seed:dev": "bunx wrangler d1 execute bingmail --local --yes --file ../db/seeds/dev.sql"
  }
}
